import RelationshipPreview from './preview';

export const metadata = { title: 'Relationship Insight — JIJI RULING', description: 'Explore communication and connection across romantic, family, friendship and working relationships.', robots: { index: false, follow: false } };

export default function CompatibilityPage() {
  return <RelationshipPreview />;
}
